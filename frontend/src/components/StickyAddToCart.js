import React from "react";

const StickyAddToCart = () => (
  <div className="sticky-cart-container">
    <button className="sticky-button">🛒 Add to Cart</button>
  </div>
);

export default StickyAddToCart;
